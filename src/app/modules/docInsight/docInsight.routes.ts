import { Routes } from "@angular/router";
import { docInsightPageComponent } from "./pages/docInsight-page/docInsight-page.component";

export const routes: Routes = [
  {
    path: '',
    component: docInsightPageComponent,
  }
]
